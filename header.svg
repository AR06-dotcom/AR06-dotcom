<svg width="850" height="210" xmlns="http://www.w3.org/2000/svg">
  <style>
    /* Text Animations */
    .name { font: bold 60px -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; fill: white; animation: fadeUp 1s ease-out forwards; opacity: 0; }
    .sub { font: bold 20px -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; fill: white; animation: fadeUp 1s ease-out 0.3s forwards; opacity: 0; }
    
    /* Background Blob Animation */
    .blob { animation: float 8s ease-in-out infinite; transform-origin: 425px 105px; }
    
    /* Keyframes */
    @keyframes fadeUp {
      0% { opacity: 0; transform: translateY(15px); }
      100% { opacity: 1; transform: translateY(0); }
    }
    @keyframes float {
      0%, 100% { transform: translateY(0) scale(1) rotate(0deg); }
      50% { transform: translateY(-10px) scale(1.05) rotate(3deg); }
    }
  </style>
  
  <rect width="100%" height="100%" fill="#0D1117" rx="10" />
  
  <path class="blob" d="M425,20 C500,10 540,80 510,140 C480,200 390,220 330,170 C270,120 310,40 425,20 Z" fill="#1C2E4A" opacity="0.9"/>

  <text x="425" y="110" class="name" text-anchor="middle">Aryan Rahate</text>
  <text x="425" y="155" class="sub" text-anchor="middle">BCA • Coding • Logic</text>
</svg>
